# Agent notes

Read `node_modules/next/dist/docs/` for this Next.js version before changing framework APIs.

- Content lives in `site/*.json` — prefer editing data over hardcoding pages.
- Only `InquiryForm` should be a client component (JS budget).
- Brand voice: parameter-first, no marketing fluff.
